<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="CarrierSpoof"
        android:allowBackup="false"
        android:theme="@android:style/Theme.DeviceDefault.DayNight">

        <meta-data android:name="xposedmodule" android:value="true"/>
        <meta-data android:name="xposeddescription" android:value="Framework-level carrier/IMSI/ICCID spoof (MuMu)"/>
        <meta-data android:name="xposedminversion" android:value="100"/>
        <meta-data android:name="xposedscope" android:resource="@array/xposed_scope"/>
        <meta-data android:name="xposedmodule_class" android:value="dev.local.carrierspoof.MainHook"/>

        <activity android:name=".PickerActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
